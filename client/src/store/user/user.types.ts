// user action types
import { userActionTypes } from "../../types/store.types";

// user action types
export const USER_ACTION_TYPES : userActionTypes = {
    SET_CURRENT_USER: 'SET_CURRENT_USER',
  };
